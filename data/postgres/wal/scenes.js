const SCENES = [
{
  num:'01', tab:'FPI', title:'찢어진 페이지를 WAL 안에 넣어 둔다',
  sub:'InnoDB 는 별도 버퍼를 두고, PG 는 로그 안에 페이지 전체를 넣는다',
  cast:['op','rec','seg','cmp'],
  knobs:[
    ['full_page_writes','on','checkpoint 후 첫 수정 때 페이지 전체를 WAL 에 넣는다'],
    ['wal_compression','off','그 페이지 이미지를 압축한다 — pglz · lz4 · zstd'],
    ['wal_log_hints','off','hint bit 만 바뀐 페이지도 이미지를 남긴다 — 논리 복제에 필요하다'] ],
  watch:[
    ['pg_stat_wal','wal_fpi 가 전체 이미지 개수 — checkpoint 직후 급증한다'],
    ['pg_waldump','fpw 표시가 붙은 레코드와 그 길이'],
    ['SELECT * FROM pg_stat_bgwriter','checkpoints_timed 대 checkpoints_req 비율'] ],
  links:[
    ['02','그 이미지를 복구가 어떻게 쓰는가'],
    ['03','checkpoint 가 조건을 다시 설정한다'] ],
  init:{
    op:{ kv:{ '문장':'UPDATE t SET v=1 WHERE id=1', '페이지':'p:7', '페이지 LSN':'0/3A00',
              'RedoRecPtr':'0/4000', '이미지 필요':'—' } },
    rec:{ items:[
      { id:'헤더', sz:1, tag:'hdr', sub:'24 B' },
      { id:'변경분', sz:1, tag:'gold', sub:'수십 B' } ],
      byl:{ l:'평소의 WAL 레코드', r:'작다 — 바뀐 것만' } },
    seg:{ items:[{ id:'000…01', tag:'hold', sub:'16 MB 세그먼트' }] },
    cmp:{ kv:{ 'InnoDB':'—', 'PG':'—' } },
  },

  steps:[
  { look:{ op:true, rec:true },
    note:'평소 WAL 레코드는 작다 — 바뀐 바이트만 적는다',
    why:'WAL 은 물리 논리 혼합 로그다. 페이지 안의 어디를 어떻게 바꿨는지만 적으므로 레코드가 수십 바이트에 그친다.',
    key:'그래서 <em>WAL 은 싸다</em>. 이 덱의 나머지는 그 싼 로그로 찢어진 페이지까지 감당할 수 있는가 하는 문제다.',
    ref:'src/backend/access/transam/xloginsert.c', sym:'XLogInsert',
    beat:1 },

  { look:{ op:true },
    note:'그런데 변경분만으로는 복구할 수 없는 경우가 있다',
    why:'GUC 설명이 그 경우를 정확히 적는다 — OS 크래시 중에 진행되던 페이지 쓰기가 디스크에 절반만 기록될 수 있고, 그때 WAL 에 저장된 행 변경만으로는 복구가 되지 않는다.',
    key:'변경분은 <em>온전한 페이지에 얹는 델타</em>다. 절반만 쓰인 페이지에 얹으면 더 망가진다 — 같은 문제를 InnoDB 도 갖는다(mysql/innodb 03).',
    ref:'src/backend/utils/misc/guc_tables.c', sym:'ConfigureNamesBool',
    fact:[['src/backend/utils/misc/guc_tables.c','gettext_noop("A page write in process during an operating system crash might be "'],
          ['src/backend/utils/misc/guc_tables.c','"stored in WAL are not enough to recover.  This option writes "']],
    ops:{ op:{ set:{ '이미지 필요':'판정 중|gold' } } } },

  { act:{ f:'op', t:'rec', lb:'페이지 LSN 과 RedoRecPtr 비교' },
    note:'판정은 한 줄이다 — 이 페이지가 checkpoint 이후 처음 수정되는가',
    why:'페이지 LSN 이 RedoRecPtr 보다 작거나 같으면 그 페이지는 마지막 checkpoint 이후 아직 수정되지 않았다는 뜻이고, 그때 이미지를 넣는다. 지금 0/3A00 ≤ 0/4000 이므로 필요하다.',
    key:'조건이 <em>checkpoint 마다 한 번</em>이 되도록 만들어져 있다. 같은 페이지를 백 번 고쳐도 이미지는 한 번만 들어간다.',
    ref:'src/backend/access/transam/xloginsert.c', sym:'XLogRecordAssemble',
    fact:[['src/backend/access/transam/xloginsert.c','needs_backup = (page_lsn <= RedoRecPtr);'],
          ['src/backend/utils/misc/guc_tables.c','gettext_noop("Writes full pages to WAL when first modified after a checkpoint."),']],
    beat:1,
    ops:{ op:{ set:{ '이미지 필요':'예  (0/3A00 ≤ 0/4000)|red' } },
          rec:{ set:{ '변경분':{ id:'변경분', sz:1, tag:'gold', sub:'수십 B' } },
                add:[{ id:'페이지 전체 이미지', sz:8, tag:'red', sub:'8,192 B  ·  FPI' }] } } },

  { look:{ rec:true },
    note:'레코드가 수십 바이트에서 8KB 로 뛴다',
    why:'이미지가 붙으면 그 레코드 하나가 페이지 한 장 크기가 된다. checkpoint 직후에 쓰기가 몰리면 WAL 생성량이 몇 배로 늘어난다.',
    key:'그래서 <em>checkpoint 간격이 WAL 양을 정한다</em> — 자주 하면 이미지가 자주 들어가고, 드물게 하면 복구가 길어진다. 어느 쪽도 공짜가 아니다.',
    ref:'src/backend/access/transam/xloginsert.c', sym:'XLogRecordAssemble',
    beat:1,
    ops:{ op:{ set:{ '페이지 LSN':'0/4120  (이제 RedoRecPtr 보다 크다)' } },
          seg:{ set:{ '000…01':{ id:'000…01', tag:'dirty', sub:'FPI 로 빠르게 찬다' } } } } },

  { act:{ f:'rec', t:'rec', lb:'구멍은 빼고 넣는다' },
    note:'8KB 를 통째로 넣지는 않는다 — 빈 공간은 뺀다',
    why:'힙 페이지의 가운데는 아직 쓰이지 않은 구멍이다. 그 구간을 빼고 앞뒤만 넣으며 BKPIMAGE_HAS_HOLE 로 표시한다. wal_compression 을 켜면 그 위에 압축까지 얹는다 — pglz · lz4 · zstd 중 하나.',
    key:'같은 보호를 <em>더 적은 바이트로</em> 산다. InnoDB 의 doublewrite 는 페이지를 그대로 두 번 쓰므로 이 최적화가 없다.',
    ref:'src/include/access/xlogrecord.h', sym:'BKPIMAGE_HAS_HOLE',
    fact:[['src/include/access/xlogrecord.h','#define BKPIMAGE_HAS_HOLE		0x01	/* page image has "hole" */'],
          ['src/include/access/xlogrecord.h','#define BKPIMAGE_COMPRESS_PGLZ	0x04']],
    ops:{ rec:{ set:{ '페이지 전체 이미지':{ id:'페이지 전체 이미지', sz:5, tag:'red', sub:'5,100 B  ·  구멍 뺀 뒤' } } } } },

  { look:{ cmp:true, rec:true },
    note:'두 엔진이 같은 문제를 다른 자리에서 풀었다',
    why:'InnoDB 는 doublewrite 버퍼라는 고정 영역에 페이지를 먼저 쓰고, 복구 때 그 사본으로 되살린다. PG 는 로그 안에 이미지를 넣어 복구가 로그만 읽으면 되게 한다.',
    key:'그래서 비용이 나타나는 자리가 다르다 — InnoDB 는 <em>평상시 쓰기가 두 배</em>이고, PG 는 <em>checkpoint 직후 WAL 이 부풀어 오른다</em>.',
    ref:'src/backend/access/transam/xloginsert.c', sym:'XLogRecordAssemble',
    beat:1,
    ops:{ cmp:{ set:{ 'InnoDB':'별도 doublewrite 버퍼 · 평상시 쓰기 2배',
                      'PG':'WAL 안의 FPI · checkpoint 직후 부풀어 오른다|gold' } } } },
  ],
},
{
  num:'02', tab:'복구', title:'로그를 앞에서 뒤로 한 번 읽는다',
  sub:'이미지가 있으면 덮어쓰고, 없으면 얹는다 — 그 판정이 레코드마다 있다',
  cast:['op','buf','seg','heap','cmp'],
  knobs:[
    ['recovery_target','(없음)','지정하지 않으면 로그 끝까지 재생한다'],
    ['max_wal_size','1GB','이 양이 복구 시간의 상한을 정한다 — checkpoint 를 부르는 문턱이다'] ],
  watch:[
    ['서버 로그','redo starts at 0/… 과 redo done at 0/…'],
    ['pg_control_checkpoint()','재시작 직후 checkpoint 위치가 어디까지 올라갔는지'] ],
  links:[
    ['01','그 이미지가 어떻게 로그에 들어갔는가'],
    ['03','어디서부터 읽을지를 checkpoint 가 정한다'] ],
  init:{
    op:{ kv:{ '상태':'재시작 중', 'redo 시작':'—', '읽은 레코드':'0', '적용':'—' } },
    buf:{ items:[{ id:'(비었다)', tag:'free', sub:'메모리는 전부 사라졌다' }] },
    seg:{ items:[
      { id:'rec A  (FPI)', tag:'hold', sub:'p:7 이미지 포함' },
      { id:'rec B', tag:'hold', sub:'p:7 변경분' },
      { id:'rec C', tag:'hold', sub:'p:9 변경분' } ] },
    heap:{ items:[
      { id:'p:7', tag:'x', sub:'절반만 쓰임  ·  깨졌다' },
      { id:'p:9', tag:'clean', sub:'LSN 0/4200' } ] },
    cmp:{ kv:{ 'InnoDB':'사본 복원 → redo 재생 (두 단계)', 'PG':'—' } },
  },

  steps:[
  { look:{ heap:true, seg:true },
    note:'디스크에 남은 것은 깨진 페이지 하나와 로그다',
    why:'p:7 은 쓰기 도중에 전원이 끊겨 절반만 기록됐다. 체크섬으로 그것을 알 수 있지만, 알아낸다고 고쳐지지는 않는다.',
    key:'여기서 <em>01 장면의 이미지가 값을 한다</em>. 로그를 읽다 보면 그 페이지의 온전한 사본이 나온다.',
    ref:'src/backend/access/transam/xlogrecovery.c', sym:'PerformWalRecovery',
    beat:1 },

  { act:{ f:'seg', t:'buf', lb:'rec A — 이미지를 복원' },
    note:'이미지가 붙은 레코드는 페이지를 읽지 않고 덮어쓴다',
    why:'BKPIMAGE_APPLY 가 붙어 있으면 복구는 디스크의 페이지를 읽지 않고 로그 안의 이미지로 버퍼를 채운다. 깨진 바이트를 볼 일이 없다.',
    key:'그래서 <em>읽기가 아니라 덮어쓰기</em>다. 깨진 페이지를 읽어야만 고칠 수 있다면 복구가 성립하지 않는다 — 이것이 그 고리를 끊는다.',
    ref:'src/backend/access/transam/xlogutils.c', sym:'XLogReadBufferForRedoExtended',
    fact:[['src/backend/access/transam/xlogutils.c','if (!RestoreBlockImage(record, block_id, page))'],
          ['src/include/access/xlogrecord.h','#define BKPIMAGE_APPLY			0x02	/* page image should be restored']],
    ops:{ buf:{ del:['(비었다)'], add:[{ id:'p:7', tag:'dirty', sub:'이미지로 복원  ·  LSN 0/4120' }] },
          op:{ set:{ 'redo 시작':'0/4000', '읽은 레코드':'1', '적용':'이미지 덮어쓰기' } } } },

  { act:{ f:'seg', t:'buf', lb:'rec B — 변경분을 얹는다' },
    note:'그다음 레코드는 변경분이다 — 이제 얹을 온전한 페이지가 있다',
    why:'이미지로 복원된 버퍼 위에 rec B 의 변경분을 적용한다. 순서가 로그 순서와 같으므로 결과가 하나로 정해진다.',
    key:'순서가 강제된다 — <em>이미지가 먼저, 변경분이 나중</em>이다. 로그가 그 순서를 이미 담고 있으므로 복구가 따로 정렬하지 않는다.',
    ref:'src/backend/access/transam/xlogrecovery.c', sym:'PerformWalRecovery',
    ops:{ buf:{ set:{ 'p:7':{ tag:'dirty', sub:'rec B 적용  ·  LSN 0/4180' } } },
          op:{ set:{ '읽은 레코드':'2', '적용':'변경분 얹기' } } } },

  { act:{ f:'seg', t:'heap', lb:'rec C — LSN 을 보고 건너뛴다' },
    note:'p:9 는 건너뛴다 — 페이지 LSN 이 이미 그 레코드보다 앞서 있다',
    why:'p:9 의 LSN 이 0/4200 이고 rec C 가 그보다 이전이면 이미 반영된 변경이다. 판정하려면 그 페이지를 한 번 읽어야 하므로, 재생 대상이 아닌 페이지도 읽히기는 한다.',
    key:'이 비교가 재생을 <em>몇 번 해도 결과가 같게</em> 만든다. 복구 중에 또 죽어도 안전한 이유이고, InnoDB 의 FIL_PAGE_LSN 비교와 같은 장치다.',
    ref:'src/backend/access/transam/xlogutils.c', sym:'XLogReadBufferForRedoExtended',
    beat:1,
    ops:{ heap:{ set:{ 'p:9':{ tag:'clean', sub:'LSN 0/4200 > rec C  ·  건너뜀' } } },
          op:{ set:{ '읽은 레코드':'3', '적용':'건너뜀 (이미 반영)' } } } },

  { look:{ buf:true, heap:true },
    note:'복구가 끝났다. 페이지는 메모리에서 더티로 남는다',
    why:'디스크의 p:7 은 아직 깨진 상태지만 문제없다 — 온전한 내용이 버퍼에 있고 WAL 에 근거가 있다. 다음 checkpoint 가 내려쓴다.',
    key:'복구가 끝난 시점에 <em>디스크가 깨끗할 필요는 없다</em>. 필요한 것은 "로그와 메모리가 정합한 상태" 다.',
    ref:'src/backend/access/transam/xlogrecovery.c', sym:'PerformWalRecovery',
    ops:{ op:{ set:{ '상태':'연결 수락|green', '적용':'—' } } } },

  { look:{ cmp:true },
    note:'InnoDB 는 두 단계, PG 는 한 단계다',
    why:'InnoDB 는 doublewrite 사본으로 페이지를 먼저 되살린 뒤 redo 를 재생한다 — 두 자료를 순서대로 읽는다. PG 는 로그 하나만 앞에서 뒤로 읽으면 되고, 이미지가 그 안에 섞여 있다.',
    key:'PG 의 복구는 <em>로그 한 번 훑기</em>로 끝난다. 대가는 그 로그가 더 크다는 것 — 01 장면에서 본 부풀어 오름이 여기서 값을 한다.',
    ref:'src/backend/access/transam/xlogrecovery.c', sym:'PerformWalRecovery',
    beat:1,
    ops:{ cmp:{ set:{ 'PG':'로그 한 번 훑기 · 이미지가 그 안에 있다|gold' } } } },
  ],
},
{
  num:'03', tab:'checkpoint', title:'checkpoint 는 조건을 다시 설정하는 일이다',
  sub:'RedoRecPtr 을 옮기면 그 뒤로 모든 페이지가 다시 "첫 수정" 이 된다',
  cast:['op','buf','seg','heap','cmp'],
  knobs:[
    ['max_wal_size','1GB','이 양이 쌓이면 checkpoint 를 부른다 — 복구 시간의 상한'],
    ['checkpoint_timeout','5min','시간으로도 부른다 — 둘 중 먼저 오는 것'],
    ['checkpoint_completion_target','0.9','간격의 이만큼에 걸쳐 나눠 내려쓴다 — 급정지를 피한다'] ],
  watch:[
    ['pg_stat_bgwriter','checkpoints_timed 가 적고 checkpoints_req 가 많으면 max_wal_size 가 작다'],
    ['pg_stat_wal','wal_fpi 가 checkpoint 직후마다 튀는 모양'],
    ['서버 로그','checkpoint complete 행의 write · sync 시간'] ],
  links:[
    ['01','RedoRecPtr 이 FPI 판정의 기준이다'],
    ['02','복구가 여기서부터 읽는다'] ],
  init:{
    op:{ kv:{ '누적 WAL':'1 GB  ·  문턱 도달', 'RedoRecPtr':'0/4000', '단계':'—', 'FPI 비율':'낮음' } },
    buf:{ items:[
      { id:'p:7', tag:'dirty', sub:'LSN 0/4180' },
      { id:'p:9', tag:'dirty', sub:'LSN 0/4210' } ] },
    seg:{ items:[{ id:'누적 1 GB', tag:'hold', sub:'0/4000 ‥ 0/8000' }] },
    heap:{ items:[{ id:'파일', tag:'clean', sub:'옛 내용' }] },
    cmp:{ kv:{ 'InnoDB':'—', 'PG':'—' } },
  },

  steps:[
  { look:{ op:true, seg:true },
    note:'문턱은 둘이다 — 쌓인 양과 지난 시간',
    why:'max_wal_size 만큼 쌓이면 부르고, checkpoint_timeout 이 지나도 부른다. 전자는 복구 시간의 상한을 정하고 후자는 유휴 서버에서도 진도가 나가게 한다.',
    key:'두 문턱이 <em>서로 다른 것을 지킨다</em>. 로그로 부른 checkpoint 가 잦으면 max_wal_size 가 작다는 신호다 — 지표에서 그 비율로 읽는다.',
    ref:'src/backend/access/transam/xlog.c', sym:'CreateCheckPoint',
    beat:1,
    ops:{ op:{ set:{ '단계':'checkpoint 시작' } } } },

  { act:{ f:'buf', t:'heap', lb:'더티 페이지를 나눠 내려쓴다' },
    note:'지금 더티인 페이지를 전부 내려쓴다 — 한 번에 몰지 않고',
    why:'checkpoint_completion_target 이 0.9 면 다음 checkpoint 까지 간격의 90% 에 걸쳐 나눠 쓴다. 한꺼번에 쏟으면 그 순간 응답이 멈춘다.',
    key:'InnoDB 의 급정지와 같은 문제이고 같은 해법이다(mysql/innodb 04) — <em>미리 조금씩</em>. 다른 것은 InnoDB 가 더티 비율로, PG 가 시간 비율로 조절한다는 점이다.',
    ref:'src/backend/access/transam/xlog.c', sym:'CreateCheckPoint',
    ops:{ buf:{ set:{ 'p:7':{ tag:'clean', sub:'내려씀' }, 'p:9':{ tag:'clean', sub:'내려씀' } } },
          heap:{ set:{ '파일':{ tag:'clean', sub:'0/4210 까지 반영됨' } } },
          op:{ set:{ '단계':'더티 페이지 내려쓰기' } } } },

  { act:{ f:'op', t:'seg', lb:'RedoRecPtr 을 옮긴다' },
    note:'그리고 기준점을 지금으로 옮긴다 — 이것이 checkpoint 의 본체다',
    why:'RedoRecPtr 이 새 위치로 간다. 다음 복구는 여기서부터 읽으면 되고, 그 앞의 세그먼트는 재활용할 수 있다.',
    key:'파일을 내려쓰는 것은 준비이고, <em>기준점을 옮기는 것이 약속</em>이다 — "이 지점까지의 모든 변경이 데이터 파일에 있다".',
    ref:'src/backend/access/transam/xlog.c', sym:'CreateCheckPoint',
    fact:[['src/backend/access/transam/xlog.c','RedoRecPtr = XLogCtl->RedoRecPtr = XLogCtl->Insert.RedoRecPtr = checkPoint.redo;']],
    beat:1,
    ops:{ op:{ set:{ 'RedoRecPtr':'0/8000|gold', '누적 WAL':'0  ·  세그먼트 재활용', '단계':'기준점 이동' } },
          seg:{ set:{ '누적 1 GB':{ id:'세그먼트 재활용', tag:'free', sub:'0/4000 이전은 버려도 된다' } } } } },

  { look:{ op:true },
    note:'그 순간 모든 페이지가 다시 "첫 수정" 대상이 된다',
    why:'01 장면의 판정은 페이지 LSN 이 RedoRecPtr 이하인가였다. 기준점이 앞으로 갔으므로 지금 디스크의 모든 페이지가 그 조건을 만족한다 — 다음에 어느 페이지를 고쳐도 이미지가 들어간다.',
    key:'그래서 <em>checkpoint 직후가 WAL 이 가장 빠르게 늘어나는 구간</em>이다. pg_stat_wal 의 wal_fpi 가 톱니 모양으로 튀는 이유가 이것이다.',
    ref:'src/backend/access/transam/xloginsert.c', sym:'XLogRecordAssemble',
    beat:1,
    ops:{ op:{ set:{ 'FPI 비율':'급증  ·  전부 첫 수정이 됐다|red' } } } },

  { look:{ cmp:true, op:true },
    note:'정리 — checkpoint 를 자주 하면 무엇이 나빠지는가',
    why:'자주 하면 복구는 짧아지지만 FPI 가 자주 들어가 WAL 양이 늘고, 드물게 하면 WAL 은 줄지만 복구가 길어지고 한 번에 내려쓸 더티 페이지가 많아진다.',
    key:'조절할 것이 하나가 아니라 <em>셋이 서로 당긴다</em> — 복구 시간 · WAL 양 · 쓰기 급증. InnoDB 도 같은 삼각형을 갖고 있고, 손잡이 이름만 다르다.',
    ref:'src/backend/access/transam/xlog.c', sym:'CreateCheckPoint',
    beat:1,
    ops:{ cmp:{ set:{ 'InnoDB':'redo 용량 · 더티 비율 · io_capacity',
                      'PG':'max_wal_size · timeout · completion_target|gold' } } } },
  ],
},
];

export { SCENES };
